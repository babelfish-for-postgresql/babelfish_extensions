/*-------------------------------------------------------------------------
 *
 * pltsql_outfuncs.c
 *    Stub — the generated _out* functions and switch dispatch are now
 *    included directly by pltsql_nodeio.c which provides the top-level
 *    pltsql_outNode() dispatcher.
 *
 *    This file is kept empty to avoid removing it from version control.
 *    It is no longer compiled (removed from Makefile OBJS).
 *
 *-------------------------------------------------------------------------
 */
